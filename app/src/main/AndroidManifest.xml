<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flighttracker20">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="Flight Tracker"
        android:theme="@style/Theme.FlightTracker20"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".RouteStatusActivity" android:exported="false" />
        <activity android:name=".FlightStatusActivity" android:exported="false" />
        <activity android:name=".FlightWebActivity" android:exported="false" />
        <activity android:name=".FlightHistoryActivity" android:exported="false" />

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
